# Google Forms credentials -- two separate auth methods, NOT a fallback chain.
# main.py uses GoogleFormsClient which ONLY supports service-account JSON.
# The production client supports both (SA first, then OAuth).
GOOGLE_SERVICE_ACCOUNT_FILE=service-account.json
# OAuth client secrets (production client only):
GOOGLE_CREDENTIALS_FILE=credentials.json

# Tallyfy credentials
TALLYFY_API_KEY=your_tallyfy_api_key_here
TALLYFY_ORGANIZATION=your_organization_subdomain
# Base URL only. The client appends /api itself, so do NOT include it here.
TALLYFY_API_URL=https://api.tallyfy.com

# AI augmentation (optional -- deterministic fallbacks run without it)
ANTHROPIC_API_KEY=sk-ant-api03-...
AI_MODEL=claude-3-haiku-20240307
AI_TEMPERATURE=0
AI_MAX_TOKENS=500
